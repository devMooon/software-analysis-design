package ch04;

public class practice3 {
	//Product 클래스(상위 클래스)의 멤버변수에 name이 없어서(X)
	//자바에서는 abstract 생성자를 만들 수 없기 때문.
	//자바에서 생성자는 상속되지 않기 때문에 abstract 생성자는 의미가 없음
	//따라서 생성자에서 제품에 이름을 붙이는 것이 아니라 이름을 붙이기 위한 메소드를 별도로 선언해야한다.
}
